# 门窗洞口可视化编辑方案

状态：已接入 Angular 工作台。正视编辑采用与三维模型共用洞口数据的墙面立面视图；完成后回到原三维视角。独立交互示意仍保留用于设计对比。

## 主流程

采用“选墙 / 选门窗洞口 → 墙面正视编辑 → 返回原三维视角”。门洞与窗户使用同一个界面、尺寸体系和撤销历史。三维与户型图用于定位墙体；正视图用于准确编辑洞口位置、宽度、高度和窗台高度。

- 选中可编辑墙体后显示“编辑门窗”。三维中选中窗户或门洞边框时也能直接进入该视图。空门洞通过正视图选中，避免三维射线穿过洞口而选中后方家具。
- 相机正对目标墙面并显示整面墙，其他房间淡出且不参与拾取。进入前记录观察状态，完成后恢复。
- 编辑对象是附属于墙的洞口及其边框、玻璃或关联门扇，不会拖走墙体或拆散组件。门扇样式与开门方向是独立设置，不混入洞口缩放操作。
- 正视图仅提供墙面内的移动。门窗编辑与现有墙体移动 / 缩放模式互斥。

## 窗户操作

- 拖窗身：水平移动、上下移动以调整窗台高度。
- 拖左右边：调整宽度；拖上下边：调整高度；拖四角：同时调整宽高，对侧边角固定。Shift 等比例调整。
- “画一扇窗”：在空白墙面拖出矩形，预览有效洞口，松手完成。
- 实时标注：窗宽、窗高、窗台离地、距墙左侧；点击标注或属性栏可输入精确厘米值。

## 门洞操作

- 拖门洞内部：沿墙左右移动，底边固定贴地 0 cm，不随鼠标上下移动而悬空。
- 提供五个手柄：左侧中点、右侧中点、顶部中点、左上角、右上角。左右边调宽，顶部调高，上角同时调宽高，对侧边和底边保持固定；Shift 可等比例调整上角。
- 底边不提供缩放手柄；“底部贴地（固定）”显示 0 cm 且不可输入。普通门洞不提供悬空或窗台选项。
- “画门洞”：在墙面从一侧拖向另一侧顶部，水平跨度决定宽度，结束点离地高度决定门洞高度，底边自动落到地面。可从左向右或从右向左绘制。
- 实时标注门洞宽、高、距墙左侧，使用与窗户相同的厘米输入框。
- 删除门洞恢复完整墙面；若有关联门框、门扇，应一并处理，撤销时作为整体恢复。
- 固定入户门属于受保护墙体，仅显示尺寸；不因采用统一编辑器而解除锁定。

## 共用操作

- 默认5厘米吸附，可切换1厘米；增加窗台、窗顶对齐线及墙面居中吸附。吸附线必须明确显示，避免用户不明白物件为何跳动。
- 选中洞口的浮动工具条提供复制、删除。删除恢复该位置墙面；复制生成同类型新洞口，放置时继续进行边界和重叠检查，门洞副本仍贴地。
- 一次拖动作为一个撤销步骤；松手后自动保存。Esc / 指针取消恢复拖动前状态。
- 手机放大边角有效触控区域，数值面板移到底部；拖动期间不弹出遮挡画面的面板。

## 约束与反馈

- 黑色实墙、外围墙和用户确认的固定墙，均只允许查看已有门窗，禁止开洞、移动、缩放、删除。
- 不允许洞口越过墙体、与其他门窗洞口重叠或倒置。示意中最小宽高为30厘米，仅用于防止拖拽退化；正式实现应按洞口类型设置合理布置约束，不代表施工标准。
- 无效预览显示红色轮廓与具体原因，已生效几何停留在最后有效位置。
- 门窗尺寸与墙面位置变化分开处理；之后移动、旋转所属墙体时，洞口与关联的边框、玻璃、门扇必须一起变化。
- 现有弧形外围玻璃继续锁定，不进入普通矩形窗编辑流程。

## 对现有程序的实现要求

1. 在墙体 / 装修面板中增加门窗正视模式，保留数值编辑作为补充。门窗可视化手柄采用所属墙面的平面坐标，不复用墙体地面坐标拖拽；按 kind 决定五个或八个手柄、是否锁定底边。
2. 原始 GLB 和动态墙体生成的门窗框、玻璃都携带稳定的 openingId；拾取时解析为所属洞口。避免洞口排序后沿用数组下标导致选错对象。
3. 进入 / 退出正视模式不改变全屋“隐藏最近墙面”及房间可见性的持久设置。
4. 更新洞口时只重建所属墙体，拖动预览不持续写存档；松手统一提交历史与布局。稳定ID需兼容旧版仅含数组下标的门窗记录。Delete 在洞口模式下只关闭选中洞口，不删除所属墙体。
5. 当前验证仅允许水平方向不重叠的洞口。若支持门上气窗或上下排列窗，需先把验证与墙面分片升级为二维矩形洞口并集；不能只放松校验，否则可能生成填回洞口的错误实体。
6. 验收：正反面坐标一致、斜墙正视、洞口与墙体独立选中、多洞口排序、边角缩放、窗台高度、门洞贴地移动与绘制、门洞删除回填、碰撞反馈、固定入户门保护、撤销 / 取消、触控、旧存档迁移、GLB导出洞口一致性。

已实现窗户八手柄缩放、门洞五手柄缩放、门洞贴地移动、画窗 / 画门洞、正反面切换、尺寸反馈及点击输入、二维重叠约束、复制、删除回填、撤销/重做、触控取消、三维几何联动及导出。稳定洞口ID兼容旧存档，固定墙保护继续有效。墙面编辑期间使用独立正视图，原相机与房间可见性不变。智能对齐线（窗台 / 窗顶 / 居中）属于后续增强，目前使用可切换的5厘米或1厘米网格吸附。
