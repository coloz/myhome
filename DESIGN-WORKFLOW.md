# 家装设计方法与软件实现

本轮把软件从三维布置工具扩展为「需求任务书 → 场景布置 → 费用估算 → 空间检查 → 三维验证」工作台。自动布置适用于当前原始清水房，模型室内多边形面积约 **103.57 ㎡**，公共光厅不计入装修面积。它是可编辑的概念方案，不是施工图。

## 设计师的工作顺序

1. **理解生活**：家庭成员、起居节奏、居家工作、访客、儿童成长、照护、收纳、预算；区分必须满足的功能与可以削减的配置。
2. **确认现状**：复测边界与层高、保护墙、门窗、湿区、设备条件。软件使用已存在的模型版本；未知现场条件列为待确认。
3. **分配空间**：为每个房间写清用途，再放置有真实尺寸的家具；优先保持入口、过道和主要活动空间。
4. **统一饰面**：依据选定场景设置墙地面，厨房及卫浴使用砖面示意；颜色与贴图不代表材料性能。
5. **迭代预算**：把家具、地面、墙面、施工暂列及预备金分开计算。超支时先调整可舍弃项，保留核心生活功能。
6. **验证与交付**：检查模型内几何问题，查看平面、三维与漫游，保存独立方案和任务书；将无法验证的内容交给深化设计与现场复核。

设计任务书的依据参考建筑师撰写的 [How to Write a Design Brief](https://www.houzz.com/magazine/how-to-write-a-design-brief-for-your-new-home-or-remodel-stsetivw-vs~82966184)。预算与方案需要反复调整，参考 [How to Refine Your Renovation Vision to Fit Your Budget](https://www.houzz.com/magazine/how-to-refine-your-renovation-vision-to-fit-your-budget-stsetivw-vs~69642354)。软件中的数值是本项目的演示参数，并非这些文章提供的价格或规范。

## 功能分析与本轮完成范围

| 设计环节 | 原有能力 | 本轮补充 |
| --- | --- | --- |
| 需求调研 | 无统一入口 | 三种家庭场景；可编辑家庭成员、需求、风格与预算 |
| 空间规划 | 拖放家具、编辑墙与门窗 | 独立场景生成、逐房用途与面积表、保留模板墙与湿区 |
| 材料与表达 | 墙地面、家具库、三维视图 | 场景饰面配置，与任务书一起保存 |
| 费用控制 | 无 | 家具分项、地面损耗、墙面毛面积、施工暂列、预备金、超支提醒 |
| 方案校验 | 户型与布局格式验证 | 旋转家具包围盒、凹多边形房间边界、家具重叠、墙体相交、门口预留、层高 |
| 比选 | 切换已有方案 | 三场景同户型比较；MCP 可比较 2–6 个保存方案 |
| AI 接入 | 无 | 14 个 MCP 工具、3 个资源、3 个提示词；原子写入与乐观并发校验 |
| 交付 | 布局 JSON / GLB / 截图 | 可移植设计任务书、预算检查 JSON、三套可重放示范及执行记录 |

## 使用

```powershell
npm install
npm run simulate:designs
npm run build
npm run serve:local
```

打开 <http://127.0.0.1:8790/>，点击顶部「家装设计」或侧栏「从生活需求开始设计」。

- 从生活场景生成预览，修改任务书与预算，查看空间任务书、检查项和费用明细，再创建独立三维方案。
- 「读取 MCP 方案」读取本地 MCP 文件；静态部署回退到随构建发布的三套示例。
- 「检查当前装修」分析打开工作台时的当前布局快照。修改装修后重新打开工作台再次检查。
- 下载的设计任务书可通过主界面「导入」创建新方案；普通布局 JSON 仍按原有规则导入当前方案。
- 浏览器副本支持编辑、撤销、导出、刷新恢复和原有云同步。新建的游客模拟草稿先保存在本地，登录后继续同步。

需求文字会随布局保存，但内置生成器按所选场景模板工作，不会自动理解任意自由文字。个性化要求可由 MCP 客户端调用家具工具继续实施，或在网页中手动修改。

## 预算与校验算法

- 坐标使用米，X/Z 为平面，Y 为高度；目录尺寸顺序为宽、深、高。布局旋转使用弧度，MCP 参数使用角度。
- 地面工程量 = 房间多边形面积 × 1.08；墙面工程量 = 周长 × 层高，暂不扣除门窗。
- 家具模拟单价按目录类别设置，乘可调整的价格系数；不是相应品牌的报价。基础施工与未建模的厨卫配置按面积暂列。
- 总价 = 各分项之和 + 按比例计提的预备金。隐藏房间或切换清水房显示不改变工程范围；删除家具才减少家具预算。
- 家具检查采用旋转矩形的分离轴检测，并考虑垂直高度；对墙段按开口分段。门口预留检查为门洞宽度乘门两侧的指定距离。
- 房间边界检查包含家具角点与边缘采样。复杂凹边界和特殊形状家具仍需人工复核。没有目录尺寸的原始家具明确列为未测量，未计入家具预算和碰撞检查。

**尚待深化**：连续通路和转身半径、门扇开启、窗户操作、儿童防夹、照护净空、防滑、采光、照度、机电点位、给排水及施工合规。缺少合适模型的水槽、淋浴、洗面台、定制橱柜等列入待深化与施工暂列，没有伪装成已完成建模。

实现核心为 `src/design-engine.ts`，网页和 MCP 调用同一份逻辑。协议配置与完整工具表见 [MCP.md](MCP.md)，三种需求的实际实施与截图见 [模拟装修记录](docs/simulations/README.md)。
